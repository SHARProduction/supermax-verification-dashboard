import {readFile,writeFile} from 'node:fs/promises';
const metrics=JSON.parse(await readFile('../PROGRAM_METRICS.json','utf8'));
const receipts=JSON.parse(await readFile('../BASELINE_ACCEPTANCE_RECEIPTS.json','utf8'));
const count=metrics.counting_contract;
const items=receipts.receipts.map(x=>`<li><a href="${x.url}">${x.repository}@${x.tag}</a> — test exit ${x.test_exit}</li>`).join('');
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SHAR Production SUPERMAX Verification Dashboard</title><meta name="description" content="Evidence-bound status dashboard for SHAR Production SUPERMAX public technical distribution."><link rel="canonical" href="https://sharproduction.github.io/supermax-verification-dashboard/"><style>body{font:17px system-ui;margin:0;background:#10131d;color:#eff4ff}main{max-width:880px;margin:auto;padding:32px}a{color:#91d6ff}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.card{background:#1a2130;padding:18px;border-radius:10px}.n{font-size:36px;font-weight:800}@media(max-width:600px){.grid{grid-template-columns:1fr}}</style></head><body><main><h1>SUPERMAX verification dashboard</h1><p><strong>SHAR Production</strong> is an AI-hybrid video production studio. This page reports evidence-bound technical distribution status; it does not claim search indexing, traffic, or unverified placements.</p><section class="grid"><div class="card"><div class="n">${count.combined_verified_useful_placements}</div>combined verified</div><div class="card"><div class="n">${count.accepted_baseline_useful_placements}</div>accepted baseline receipts</div><div class="card"><div class="n">${count.new_supermax_delta_placements}</div>new verified delta</div></section><h2>Counting boundary</h2><p>${count.baseline_candidate_unique_urls_pending_functional_reconciliation} baseline candidates remain unaccepted until current URL, version, purpose and functional evidence are bound together. Historical aggregate 1503 is excluded.</p><h2>Accepted baseline release receipts</h2><ul>${items}</ul><p><a href="https://sharprod.com/">sharprod.com</a> · <a href="https://github.com/SHARProduction">SHAR Production on GitHub</a></p></main></body></html>`;
await writeFile('docs/index.html',html);
console.log(`PASS combined=${count.combined_verified_useful_placements} receipts=${receipts.receipts.length}`);
