import {readFile,writeFile} from 'node:fs/promises';
const metrics=JSON.parse(await readFile('data/PROGRAM_METRICS.json','utf8'));
const receipts=JSON.parse(await readFile('data/BASELINE_ACCEPTANCE_RECEIPTS.json','utf8'));
const ledger=JSON.parse(await readFile('data/VERIFIED_PLACEMENTS.json','utf8'));
const submissions=JSON.parse(await readFile('data/EXTERNAL_SUBMISSIONS.json','utf8'));
if(!Array.isArray(ledger.placements)||ledger.counted_placements!==ledger.placements.length)throw new Error('Ledger count mismatch');
if(new Set(ledger.placements.map(x=>x.placement_id)).size!==ledger.placements.length)throw new Error('Ledger placement IDs are not unique');
if(!Array.isArray(submissions.submissions))throw new Error('External submission ledger is invalid');
if(!Array.isArray(receipts.receipts)||receipts.accepted_count!==receipts.receipts.length)throw new Error('Baseline receipt count mismatch');
if(new Set(receipts.receipts.map(x=>x.url)).size!==receipts.receipts.length)throw new Error('Baseline receipt URLs are not unique');
const base=metrics.counting_contract.accepted_baseline_useful_placements;
if(base!==receipts.receipts.length)throw new Error('Baseline metric does not match row-level receipts');
const combined=base+ledger.placements.length;
const count={...metrics.counting_contract,new_supermax_delta_placements:ledger.placements.length,combined_verified_useful_placements:combined,remaining_to_3000:Math.max(0,3000-combined)};
const receiptPreview=receipts.receipts.slice(0,20);
const items=receiptPreview.map(x=>`<li><a href="${x.url}">${x.repository}${x.tag?`@${x.tag}`:''}</a> — ${x.verification_reason ?? `test exit ${x.test_exit}`}</li>`).join('');
const pending=submissions.submissions.filter(x=>x.status==='SUBMITTED').length;
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SHAR Production SUPERMAX Verification Dashboard</title><meta name="description" content="Evidence-bound status dashboard for SHAR Production SUPERMAX public technical distribution."><link rel="canonical" href="https://sharproduction.github.io/supermax-verification-dashboard/"><style>body{font:17px system-ui;margin:0;background:#10131d;color:#eff4ff}main{max-width:880px;margin:auto;padding:32px;overflow-wrap:anywhere}a{color:#91d6ff}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.card{background:#1a2130;padding:18px;border-radius:10px}.n{font-size:36px;font-weight:800}@media(max-width:600px){main{padding:18px}.grid{grid-template-columns:1fr}}</style></head><body><main><h1>SUPERMAX verification dashboard</h1><p><strong>SHAR Production</strong> is an AI-hybrid video production studio. This page reports evidence-bound technical distribution status; it does not claim search indexing, traffic, or unverified placements.</p><section class="grid"><div class="card"><div class="n">${count.combined_verified_useful_placements}</div>combined verified</div><div class="card"><div class="n">${count.accepted_baseline_useful_placements}</div>accepted baseline receipts</div><div class="card"><div class="n">${count.new_supermax_delta_placements}</div>row-level verified delta</div></section><h2>External submission queue</h2><p>${pending} submitted external listing${pending===1?'':'s'} awaiting independent provider review. Submitted listings do not change the verified count.</p><h2>Counting boundary</h2><p>The delta is derived at build time from ${ledger.placements.length} unique row-level evidence records. ${count.baseline_candidate_unique_urls_pending_functional_reconciliation} baseline candidates remain unaccepted until current URL, version, purpose and functional evidence are bound together. Historical aggregate 1503 is excluded.</p><h2>Accepted baseline receipts</h2><p>Showing ${receiptPreview.length} of ${receipts.receipts.length}; every accepted receipt remains available in the row-level ledger.</p><ul>${items}</ul><p><a href="https://sharprod.com/">sharprod.com</a> · <a href="https://github.com/SHARProduction">SHAR Production on GitHub</a></p></main></body></html>`;
await writeFile('docs/index.html',html);
console.log(`PASS combined=${count.combined_verified_useful_placements} receipts=${receipts.receipts.length}`);
